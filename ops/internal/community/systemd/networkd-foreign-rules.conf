# egress.sh adds routing rules networkd didn't create, and restarting networkd deletes
# those by default. Unattended upgrades restart it. Lives in /etc/systemd/networkd.conf.d/.
[Network]
ManageForeignRoutingPolicyRules=no
ManageForeignRoutes=no
